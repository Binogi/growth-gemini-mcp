# @google/genai 2.x declares an optional peer on @modelcontextprotocol/sdk ^1.25.2 while this
# project pins the SDK at 1.22.0 (see CLAUDE.md). The pair works at runtime; the genai MCP
# helper that wants the newer SDK is not used here. Tell npm to accept the peer mismatch.
legacy-peer-deps=true
